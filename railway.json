{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @project/db exec prisma generate && pnpm --filter web build && pnpm --filter server build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @project/db exec prisma db push --skip-generate && node apps/server/dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
