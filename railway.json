{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @project/db exec prisma generate && pnpm sdk:generate && pnpm --filter web build && pnpm --filter server build"
  },
  "deploy": {
    "startCommand": "sh -c 'timeout 30 pnpm --filter @project/db exec prisma db push --skip-generate || true' && pnpm --filter server start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
